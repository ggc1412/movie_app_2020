import React from "react";
// JSX를 사용하기 위해 React를 import 한다.
// JSX는 javascript 안의 HTML 형태
// React에서 Component를 사용해서 HTML를 작성하기 위해 사용한다.

function First() {
    return <h3>It is first Component</h3>;
}

export default First;